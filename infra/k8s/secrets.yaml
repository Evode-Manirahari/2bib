# WARNING: DO NOT commit real credentials.
# Use Sealed Secrets, External Secrets Operator, or Vault to inject real values.
# This file is a template — replace CHANGE_ME placeholders before applying.
#
# Apply with: kubectl apply -f secrets.yaml -n pe
apiVersion: v1
kind: Secret
metadata:
  name: pe-secrets
  namespace: pe
  labels:
    app.kubernetes.io/name: pe
type: Opaque
stringData:
  DATABASE_URL: "postgresql://pe:CHANGE_ME@postgres:5432/pe"
  STRIPE_SECRET_KEY: "sk_live_CHANGE_ME"
  STRIPE_WEBHOOK_SECRET: "whsec_CHANGE_ME"
  ANTHROPIC_API_KEY: "sk-ant-CHANGE_ME"
